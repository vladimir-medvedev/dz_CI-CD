---
- name: Install Kibana
  apt:
    name=kibana
    state=latest
  notify:
    - kibana systemd

- name: kibana
  copy: src=/etc/ansible/roles/kibana/files/kibana.yml dest=/etc/kibana/kibana.yml
  notify:
    - restart kibana
